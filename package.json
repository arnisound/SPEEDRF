{
  "name": "speedrf",
  "version": "0.1.0-beta",
  "description": "SPEED RF — plan de fréquences micros HF en un clic (instance simplifiée de RF SHOT by Arnisound tools).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server --directory src 8080",
    "preview": "python3 -m http.server --directory dist 8081"
  },
  "devDependencies": {
    "terser": "^5.48.0"
  }
}
